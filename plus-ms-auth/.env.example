PORT=3001
JWT_SECRET=change-me

AWS_ACCESS_KEY_ID=test
AWS_SECRET_ACCESS_KEY=test
AWS_DEFAULT_REGION=us-east-1
AWS_ENDPOINT=http://localhost:4566

# Use "ministack-rds-plus-auth-db" no docker-compose (Ministack provisiona);
# "localhost" só funciona se rodar npm start fora do Docker com Postgres local.
DB_HOST=ministack-rds-plus-auth-db
DB_PORT=5432
DB_USER=plus
DB_PASSWORD=plus_secret
DB_NAME=plus_auth
